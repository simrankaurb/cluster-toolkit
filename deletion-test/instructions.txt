Advanced Deployment Instructions
================================

Terraform group 'primary' was successfully created in directory deletion-test/primary
To deploy, run the following commands:

terraform -chdir=deletion-test/primary init
terraform -chdir=deletion-test/primary validate
terraform -chdir=deletion-test/primary apply
gcluster export-outputs deletion-test/primary

Terraform group 'build_script' was successfully created in directory deletion-test/build_script
To deploy, run the following commands:

gcluster import-inputs deletion-test/build_script
terraform -chdir=deletion-test/build_script init
terraform -chdir=deletion-test/build_script validate
terraform -chdir=deletion-test/build_script apply
gcluster export-outputs deletion-test/build_script

Packer group was successfully created in directory deletion-test/slurm-build
To deploy, run the following commands:

gcluster import-inputs deletion-test/slurm-build
cd deletion-test/slurm-build/slurm-image
packer init .
packer validate .
packer build .
cd -

Terraform group 'cluster' was successfully created in directory deletion-test/cluster
To deploy, run the following commands:

gcluster import-inputs deletion-test/cluster
terraform -chdir=deletion-test/cluster init
terraform -chdir=deletion-test/cluster validate
terraform -chdir=deletion-test/cluster apply

Destroying infrastructure when no longer needed
===============================================

Automated
---------

gcluster destroy deletion-test

Advanced / Manual
-----------------
Infrastructure should be destroyed in reverse order of creation:

terraform -chdir=deletion-test/cluster destroy
terraform -chdir=deletion-test/build_script destroy
terraform -chdir=deletion-test/primary destroy

Please browse to the Cloud Console to remove VM images produced by Packer.
If this file is present, the names of images can be read from it:

deletion-test/slurm-build/slurm-image/packer-manifest.json

https://console.cloud.google.com/compute/images
